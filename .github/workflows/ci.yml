name: content-service-ci

on:
  push:
    branches: [master, develop]
  pull_request:
    branches: [master, develop]

permissions:
  contents: read
  packages: read

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'
      - name: Restore
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          export NuGetPackageSourceCredentials_github="Username=${GITHUB_ACTOR};Password=${GITHUB_TOKEN};ValidAuthenticationTypes=Basic"
          dotnet restore DholeContentService.slnx --configfile NuGet.Config
      - name: Build
        run: dotnet build DholeContentService.slnx -c Release --no-restore
      - name: Test
        run: dotnet test tests/Dhole.Content.UnitTests/Dhole.Content.UnitTests.csproj -c Release --no-build
